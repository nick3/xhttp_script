listen: :${HYSTERIA_PORT}

tls:
  cert: ${HYSTERIA_CERT_PATH}
  key: ${HYSTERIA_KEY_PATH}

auth:
  type: password
  password: ${HYSTERIA_AUTH_PASSWORD}

masquerade:
  type: proxy
  proxy:
    url: ${HYSTERIA_MASQUERADE_PROXY_URL}
    rewriteHost: true
